feat: sistema de tres modos de agente (prompt / workflow / mixed)

PARTE 1 - Migracion SQL (20260526_add_workflow_to_agent_config.sql):
  Añade agent_mode, workflow_definition (JSONB) y workflow_variables (JSONB)
  a agent_config. RLS existente cubre las columnas automaticamente.
  Migracion aplicada en produccion (afrrxeibtrwjaiqmhytu).

PARTE 2 - backend/utils/workflow_compiler.py:
  compile_workflow_to_prompt() convierte el grafo JSON en un system-prompt
  numerado paso a paso y una lista de pasos normalizados. Se ejecuta UNA
  sola vez al arrancar la sesion (en DynamicAgent.__init__).

PARTE 3 - backend/utils/workflow_state.py:
  WorkflowStateMachine: maquina de estados ligera que vive en memoria
  durante la llamada. Evaluacion segura de condiciones (ast.Compare,
  solo operadores permitidos). Metodos: current_step, advance,
  set_variable, get_variables, is_finished.

PARTE 4 - backend/agent.py:
  DynamicAgent.__init__ lee agent_mode y si es workflow/mixed compila el
  workflow y crea la maquina de estados. _guardar_encuesta_impl fusiona
  variables del workflow en datos_extra. CallSession.setup_events lanza
  _handle_workflow_turn en cada turno de usuario cuando hay workflow activo.
  Nuevo metodo _handle_workflow_turn procesa message/question/condition/
  llm_free/transfer/end sin romper el modo prompt existente.

PARTE 5 - backend/routers/agents.py + campaigns.py + models/schemas.py:
  Nuevos tipos Pydantic: WorkflowNode, WorkflowEdge, WorkflowDefinition,
  AgentModeConfig, WorkflowValidateRequest, WorkflowValidateResponse.
  PUT /agents guarda agent_mode/workflow_definition/workflow_variables.
  POST /api/agents/{id}/workflow/validate: previsualiza el prompt compilado
  y valida el workflow sin persistir nada.
  GET /agent_config_by_survey incluye los tres campos nuevos.

PARTE 6 - src/views/AgentFormView.tsx:
  Selector de modo (Prompt / Workflow / Mixto) con cards visuales.
  En modo prompt: textarea existente sin cambios.
  En modo workflow/mixed: WorkflowEditor + campo de contexto global.
  Boton "Previsualizar guion" llama al endpoint validate y muestra el
  prompt compilado en un modal.

PARTE 7 - src/components/WorkflowEditor.tsx:
  Editor visual con @xyflow/react. Panel lateral de nodos, handles de
  conexion, colores por tipo (message=azul, question=morado,
  condition=ambar, llm_free=verde, transfer=naranja, end=gris).
  Validacion en tiempo real (nodo inicial, nodo fin, edges de salida).
  Badge "INICIO" en nodo inicial. Sub-prompt textarea para nodos llm_free
  en modo mixed.
