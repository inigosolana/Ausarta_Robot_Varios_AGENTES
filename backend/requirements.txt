# ============================================================
# Dependencias fijadas con versión exacta (==) para garantizar
# reproducibilidad en producción y evitar breaking changes.
# Para actualizar: cambiar la versión, probar en dev, y hacer commit.
# Versiones validadas: 2026-05 (PyPI)
# ============================================================

fastapi==0.111.0
uvicorn==0.30.1
PyJWT==2.10.1

# LiveKit Agents y plugins de IA (conjunto compatible verificado en PyPI)
livekit-agents==0.12.21
livekit-plugins-openai==0.12.4
livekit-plugins-deepgram==0.7.4
livekit-plugins-cartesia==0.4.12
livekit-plugins-silero==0.7.6
livekit-plugins-noise-cancellation==0.2.5

# Cola de tareas y caché
redis==4.6.0
arq==0.26.0

# Utilidades
python-dotenv==1.0.1
requests==2.32.3
aiohttp==3.10.11

# Base de datos
mysql-connector-python==8.4.0
supabase==2.28.0

# Seguridad y rate-limiting
slowapi==0.1.9
cryptography==42.0.7

# OpenAI SDK (livekit-plugins-openai requiere >=1.68.2)
openai==1.68.2
