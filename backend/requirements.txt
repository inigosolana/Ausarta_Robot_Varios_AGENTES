# ============================================================
# Dependencias fijadas con versión exacta (==) para garantizar
# reproducibilidad en producción y evitar breaking changes.
# Para actualizar: cambiar la versión, probar en dev, y hacer commit.
# Versiones validadas: 2026-05 (PyPI)
# ============================================================

fastapi==0.111.0
uvicorn==0.30.1
PyJWT==2.10.1

# LiveKit Agents y plugins de IA (agent.py requiere API 1.x: Agent, AgentServer, etc.)
livekit-agents==1.4.6
livekit-plugins-openai==1.4.6
livekit-plugins-deepgram==1.4.6
livekit-plugins-cartesia==1.4.6
livekit-plugins-silero==1.4.6
livekit-plugins-noise-cancellation==0.2.5

# Cola de tareas y caché
redis==4.6.0
arq==0.26.0

# Utilidades
python-dotenv==1.0.1
requests==2.32.3
# livekit-agents 1.4.x usa ClientSession(proxy=...); requiere aiohttp >=3.11 (no está en 3.10.x)
aiohttp==3.14.1

# Base de datos
mysql-connector-python==8.4.0
supabase==2.28.0

# Seguridad y rate-limiting
slowapi==0.1.9
cryptography==42.0.7

# OpenTelemetry — trazabilidad distribuida (OTLP → Jaeger / Grafana Tempo)
# Alineado con livekit-agents 1.4.x (opentelemetry-api~=1.39)
opentelemetry-api==1.39.1
opentelemetry-sdk==1.39.1
opentelemetry-exporter-otlp-proto-grpc==1.39.1
opentelemetry-exporter-otlp-proto-http==1.39.1
opentelemetry-instrumentation-fastapi==0.60b1
opentelemetry-instrumentation-aiohttp-client==0.60b1

# OpenAI SDK (livekit-plugins-openai 1.4.x requiere openai>=2)
openai==2.15.0
pydantic-settings==2.10.1
fpdf2==2.8.3
pytest==8.3.5
pytest-asyncio==0.26.0
openpyxl==3.1.5
python-docx==1.1.2
pypdf==5.6.0
