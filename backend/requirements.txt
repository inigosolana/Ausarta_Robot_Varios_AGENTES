# ============================================================
# Dependencias fijadas con versión exacta (==) para garantizar
# reproducibilidad en producción y evitar breaking changes.
# Para actualizar: cambiar la versión, probar en dev, y hacer commit.
# Versiones validadas: 2026-05
# ============================================================

fastapi==0.111.0
uvicorn==0.30.1
PyJWT==2.8.0

# LiveKit Agents y plugins de IA
livekit-agents==0.11.2
livekit-plugins-openai==0.10.2
livekit-plugins-deepgram==0.6.1
livekit-plugins-cartesia==0.6.1
livekit-plugins-silero==0.5.1
livekit-plugins-google==0.6.0
livekit-plugins-noise-cancellation==0.5.0

# Cola de tareas y caché
redis==5.0.4
arq==0.26.0

# Utilidades
python-dotenv==1.0.1
requests==2.32.3
aiohttp==3.9.5

# Base de datos
mysql-connector-python==8.4.0
supabase==2.5.0
postgrest==0.16.4

# Seguridad y rate-limiting
slowapi==0.1.9
cryptography==42.0.7

# OpenAI SDK
openai==1.30.5
