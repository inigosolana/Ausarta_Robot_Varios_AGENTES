# Archivo vacío a propósito — Portainer (deploy desde Git) exige que exista si usas env_file: stack.env
# Las variables reales las defines en Portainer → Stacks → Environment variables (tu tabla name/value).
# No pongas secretos aquí ni lo subas con valores reales.
